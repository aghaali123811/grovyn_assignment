# Replicate API token — powers the AI hair try-on in Live Mode.
# Create one at https://replicate.com/account/api-tokens
#
# Without a valid token, Live Mode reports a configuration error and the app
# offers Demo Mode instead, which runs the full journey with no credential.
REPLICATE_API_TOKEN=r8_your_real_token_here
