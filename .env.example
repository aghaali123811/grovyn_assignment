# Replicate API token — powers the AI hair try-on.
# Create one at https://replicate.com/account/api-tokens
# Without a valid token the app falls back to Demo Mode (see README).
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Public base URL of the app. Used for share links.
NEXT_PUBLIC_APP_URL=http://localhost:3000
