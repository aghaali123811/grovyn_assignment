import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "LustraHair - AI Virtual Hair Try-On",
  description:
    "See your next look before you buy. Upload a photo and preview different hair styles, extensions, and colors with our AI-powered virtual try-on experience.",
  keywords: ["hair extensions", "wigs", "virtual try-on", "AI hair", "LustraHair"],
};

export default function RootLayout({
  children,
}: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
